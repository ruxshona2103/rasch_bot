{$DOMAIN} {
    handle /api/* {
        reverse_proxy bot:8080
    }

    handle_path /admin* {
        root * /srv/admin
        try_files {path} /index.html
        file_server
    }

    handle {
        root * /srv/student
        try_files {path} /index.html
        file_server
    }
}
